import React from 'react';
import { Text, View } from 'react-native';

interface Props {
  className?: string;
}

export const GroupScheduleItem: React.FC<Props> = ({ className }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
